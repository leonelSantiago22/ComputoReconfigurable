module flop (	input logic clk,
					input logic [3:0] d,
					output logic [3:0] q);
			always_ff @(posedge clk) //Always flip flop el arroba y la lista de paramatros 
			//Activacion del registro atravez de la senal de reloj 
				q<= d; //Asignacion no bloqueante
endmodule 